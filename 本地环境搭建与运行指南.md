# 无人机云平台项目本地环境搭建与运行指南

本文用于在 Windows 电脑上从零启动本项目的前端、后端和基础服务。按本文步骤完成后，可通过浏览器访问本地平台并使用演示账号登录。

> 适用目录：C:\研二上\无人机项目。若项目在其他位置，只需将命令中的路径替换为实际路径。

## 1. 项目组成

| 模块 | 目录或服务 | 技术栈 | 本地地址 |
|---|---|---|---|
| 前端 | Cloud-API-Demo-Web-main\Cloud-API-Demo-Web-main | Vue 3、Vite | http://localhost:8081 |
| 后端 | DJI-Cloud-API-Demo-main\DJI-Cloud-API-Demo-main\Service_Demo | Java 11、Spring Boot | http://localhost:45001 |
| MySQL | Docker 服务 mysql | MySQL 8 | localhost:3307 |
| Redis | Docker 服务 redis | Redis 6 | localhost:6380 |
| MQTT | Docker 服务 emqx | EMQX 4.4 | localhost:1883、8083 |
| 对象存储 | Docker 服务 minio | MinIO | http://localhost:9001 |

本地运行必须使用 docker-compose.local.yml，不要直接使用仓库原有的 docker-compose.yml。原文件是历史镜像版后端的部署配置，不适合源码本地开发。

## 2. 首次准备

### 2.1 必需软件

| 软件 | 建议版本 | 用途 |
|---|---|---|
| JDK | Temurin/OpenJDK 11 | 编译后端 |
| Maven | 3.9.x | 构建后端 |
| Node.js | 18+（已验证 Node 22） | 启动前端 |
| Docker Desktop | 当前稳定版，Linux containers 模式 | 启动基础服务与后端 |

新开一个 PowerShell，执行：

~~~powershell
java -version
javac -version
mvn -version
node -v
npm -v
docker version
docker compose version
~~~

docker version 必须同时显示 Client 和 Server。只有 Client 时，请启动 Docker Desktop 并等待状态变为 Engine running。

### 2.2 端口说明

本机常见的 3306、8080 可能已被其他软件占用，因此本地方案使用：

- MySQL：宿主机 3307，容器内 3306。
- Redis：宿主机 6380，容器内 6379。
- 前端：8081。
- 后端：45001。

检查端口：

~~~powershell
Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue |
  Where-Object { $_.LocalPort -in 3307,6380,8081,8083,9000,9001,18083,45001 } |
  Select-Object LocalAddress,LocalPort,OwningProcess
~~~

## 3. 首次启动基础服务和演示数据

进入后端根目录：

~~~powershell
cd "C:\研二上\无人机项目\DJI-Cloud-API-Demo-main\DJI-Cloud-API-Demo-main"
~~~

### 3.1 启动 MySQL、Redis、EMQX、MinIO

首次执行会下载镜像，请保持网络通畅。

~~~powershell
docker compose -f docker-compose.local.yml up -d mysql redis emqx minio
docker compose -f docker-compose.local.yml ps
~~~

确认 MySQL、Redis 状态为 healthy 后再继续。若仍为 starting，等待十几秒后重试。

### 3.2 导入演示数据库

项目自带初始数据库脚本，包含账号、工作空间和设备字典等演示数据。

~~~powershell
Get-Content -Raw -Encoding UTF8 ".\Service_Demo\sql\cloud_sample.sql" |
  docker compose -f docker-compose.local.yml exec -T mysql mysql -uroot -proot
~~~

检查是否导入成功：

~~~powershell
docker compose -f docker-compose.local.yml exec -T mysql mysql -uroot -proot -e "SELECT username, user_type FROM cloud_sample.manage_user;"
~~~

默认 Web 演示账号：

~~~text
用户名：adminPC
密码：adminPC
~~~

仅限本地演示，禁止暴露到公网。

### 3.3 创建 MinIO bucket

后端需要名为 sdu-dji-oss 的 bucket。首次运行执行一次：

~~~powershell
docker run --rm --network dji-local-network --entrypoint /bin/sh minio/mc -c "mc alias set local http://minio:9000 minioadmin minioadmin && mc mb --ignore-existing local/sdu-dji-oss"
~~~

也可手工操作：打开 http://localhost:9001，以 minioadmin / minioadmin 登录，创建 sdu-dji-oss bucket。

## 4. 构建并启动后端

项目已提供以下本地运行文件，无需手动修改：

- Service_Demo/src/main/resources/application-docker.yml：后端容器配置，使用 Docker 服务名访问 MySQL、Redis、EMQX、MinIO。
- Service_Demo/Dockerfile.local：本地后端镜像。
- docker-compose.local.yml：完整本地编排。

后端以 Docker 容器方式运行，而不是在 Windows 主机上直接运行 WAR。这能避开 Windows 到 Docker MySQL 的端口代理/JDBC 握手问题。

### 4.1 编译

~~~powershell
mvn -pl Service_Demo -am package -DskipTests
~~~

成功后会生成：

~~~text
Service_Demo\target\ServiceDemo-1.0-SNAPSHOT.war
~~~

首次构建会下载 Maven 依赖。日志中可能出现旧仓库证书、LATEST/RELEASE 依赖等警告；最终出现 BUILD SUCCESS 即可。

### 4.2 构建并启动后端容器

~~~powershell
docker compose -f docker-compose.local.yml up -d --build backend
docker compose -f docker-compose.local.yml ps
docker logs --tail 50 dji-local-backend
~~~

后端成功标志：

~~~text
Tomcat started on port(s): 45001
Started CloudApiSampleApplication
~~~

验证登录接口：

~~~powershell
$payload = '{ "username": "adminPC", "password": "adminPC", "flag": 1 }'
$response = Invoke-WebRequest -Uri "http://127.0.0.1:45001/manage/api/v1/login" -Method Post -ContentType "application/json" -Body $payload -UseBasicParsing
($response.Content | ConvertFrom-Json).code
~~~

输出 0 表示登录成功。

## 5. 启动前端

另开一个 PowerShell：

~~~powershell
cd "C:\研二上\无人机项目\Cloud-API-Demo-Web-main\Cloud-API-Demo-Web-main"
npm ci
npm run serve -- --port 8081
~~~

浏览器打开：

~~~text
http://localhost:8081
~~~

前端的本地接口地址已配置在 src/api/http/config.ts：

~~~ts
baseURL: 'http://localhost:45001/'
websocketURL: 'ws://localhost:45001/api/v1/ws'
~~~

若浏览器之前访问过旧服务，请按 Ctrl + F5 强制刷新；仍异常时清理该站点的 Local Storage 后重新登录。

## 6. 日常启动与代码更新

后续启动无需重复导入 SQL，也无需重复 npm ci。

### 后端与基础服务

~~~powershell
cd "C:\研二上\无人机项目\DJI-Cloud-API-Demo-main\DJI-Cloud-API-Demo-main"
docker compose -f docker-compose.local.yml up -d
docker compose -f docker-compose.local.yml ps
~~~

修改 Java 代码或 application-docker.yml 后：

~~~powershell
mvn -pl Service_Demo -am package -DskipTests
docker compose -f docker-compose.local.yml up -d --build backend
~~~

### 前端

~~~powershell
cd "C:\研二上\无人机项目\Cloud-API-Demo-Web-main\Cloud-API-Demo-Web-main"
npm run serve -- --port 8081
~~~

## 7. 迁移已有平台数据（可选）

仓库自带 SQL 只是演示数据。要让本地页面显示现有平台的设备、地图、航线、任务和媒体记录，需要由拥有远端数据授权的管理员迁移数据。

建议顺序：

1. 备份本地 cloud_sample。
2. 导出远端 cloud_sample。
3. 导入本地 MySQL 容器。
4. 按需同步 Redis 设备状态快照。
5. 按需同步 MinIO bucket 的对象文件。

导入远端 SQL 前，先备份本地库：

~~~powershell
docker compose -f docker-compose.local.yml exec -T mysql sh -c "mysqldump -uroot -proot --single-transaction --routines --events --databases cloud_sample > /tmp/cloud_sample-backup.sql"
~~~

远端 MinIO 中的图片、视频和航线附件可能很大。迁移前先统计大小，确认本机有“对象总大小 + 预留空间”。数据库中的媒体记录已导入但本地 MinIO 没有对应文件时，列表可见，但原图、视频或附件预览会失败。

未经授权，不得复制远端数据库、Redis 或对象存储内容。

## 8. 本地平台与真机联调的区别

本文保证本地“平台软件栈”可运行，但真机实时数据不会自动出现。

连接 DJI Pilot 2、机场或无人机，还需要：

- 让后端使用真机可访问的局域网或公网地址，不能使用 localhost。
- 配置防火墙、HTTPS、回调地址和可达端口。
- 使用自己的 DJI Cloud API 应用信息和 License。
- 将 Pilot 2/机场侧 MQTT、回调和直播地址指向该环境。
- 如需直播，额外部署 SRS/RTMP/WebRTC 服务。

本地 Redis 快照只能保留最后一次状态；持续在线位置、遥测和控制反馈需要真机持续向本地 EMQX 发布消息。

## 9. 常见问题

### java 或 mvn 找不到

安装 JDK/Maven 后关闭所有旧 PowerShell，再打开新窗口。确认 JAVA_HOME 指向 JDK 11 根目录，Path 包含 %MAVEN_HOME%\bin。

### Docker 无法连接

启动 Docker Desktop，等待 Engine running，执行：

~~~powershell
docker ps
~~~

### 后端未启动

按顺序检查：

~~~powershell
docker compose -f docker-compose.local.yml ps
docker logs --tail 100 dji-local-backend
docker logs --tail 100 dji-local-mysql
~~~

重点确认 MySQL、Redis 为 healthy，并且已导入 cloud_sample。

### 前端能打开但没有数据

1. 确认后端 http://localhost:45001 可访问。
2. 按 Ctrl + F5 强制刷新。
3. 清理旧服务的浏览器缓存后重新登录。
4. 确认已导入 SQL；演示 SQL 只有少量演示数据。
5. 若要看到原平台历史数据，按第 7 节进行授权迁移。

### 地图底图为空

项目使用高德地图 Key。网络不可用、Key 过期、域名/IP 白名单不匹配时，业务数据仍可加载，但地图底图可能为空。请换成自己申请并配置白名单的高德地图 Key。

### 8081 被占用

换一个端口：

~~~powershell
npm run serve -- --port 8082
~~~

然后访问 http://localhost:8082。后端地址不需要改变。

## 10. 停止与数据保留

停止前端：在前端 PowerShell 中按 Ctrl + C。

停止后端和基础服务、保留 MySQL/MinIO 数据：

~~~powershell
cd "C:\研二上\无人机项目\DJI-Cloud-API-Demo-main\DJI-Cloud-API-Demo-main"
docker compose -f docker-compose.local.yml stop
~~~

再次启动：

~~~powershell
docker compose -f docker-compose.local.yml up -d
~~~

MySQL 和 MinIO 数据保存在后端目录的 data-local 下。不要随意删除该目录；删除后会丢失本地数据库和对象存储数据。

